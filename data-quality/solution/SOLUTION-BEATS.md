# Solution beats — Plan Quality Remediation exercise (record in your voice)

- Run the suite; walk the failing expectations across the six dimensions.
- Classify each: PII validity -> quarantine; mandatory-attr/integrity/consent -> source; unit and
  staleness -> downstream.
- Prioritize: quarantine PII contact data first (GDPR + harm), then source, then downstream.
- Land it: tie strategies to contract SLOs; consent (~37%) and carbon unit are recurring drift for
  the dashboard, not one-time fixes.
