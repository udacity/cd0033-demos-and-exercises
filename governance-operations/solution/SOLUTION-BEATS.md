# Solution beats — Run Governance as an Operation exercise (record in your voice)

- Frame governance as a loop, not a project: one audit, many signals, drive to compliance.
- `make_catalog`, confirm `live`; run `governance_audit`, read the signal breakdown.
- Remediate the catalog in Glue (owner/classification/retention + classify PII columns); show
  catalog gaps drop to zero.
- Note the twist: classifying PII columns surfaces access-control obligations — govern every PII
  table in Lake Formation (LF-Tag + steward grant).
- Re-audit: missing_metadata and access_control_gap both zero — that's compliance for the
  AWS-fixable signals.
- Land the judgment: quality, consent, and lineage are ongoing SLOs for a dashboard, not
  one-time fixes. Close on the operating model — schedule, owners, trending.
